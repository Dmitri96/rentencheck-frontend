/**
 * Admin feature public surface.
 *
 * Phase 8 splits src/components/admin/pension-settings-panel.tsx (562 LOC)
 * into a usePensionSettings hook + PensionSettingsPanel and exports them.
 */
export {};
